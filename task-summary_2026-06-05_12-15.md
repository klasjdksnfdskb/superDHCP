# 2026-06-05 12:10 — 收尾修复：AuditLog 去重 + ExcludeCreate 前向引用

## 发现并修复了模型重组中的两个回归问题

### 1. 重复的 AuditLog 表
- **问题**：`models/audit.py`（独立文件）定义了 AuditLog，但 `models/user.py` 仍保留了旧版副本。两者均声明 `__tablename__ = "audit_logs"` → SQLAlchemy 在加载时抛出 `InvalidRequestError: Table 'audit_logs' is already defined`。
- **修复**：
  - 从 `models/user.py` 中移除 AuditLog 类定义
  - 将 `models/__init__.py` 指向 `from .audit import AuditLog`
  - 将 `routers/auth.py` 的导入拆分为 `from models.user import User, UserRole` + `from models.audit import AuditLog`

### 2. ExcludeCreate 前向引用
- **问题**：`SubnetCreate.excludes: Optional[List[ExcludeCreate]]` 引用了在其下方定义的 `ExcludeCreate`，FastAPI 加载时导致 `NameError`。
- **修复**：将 `ExcludeCreate` 定义移至 `SubnetCreate` 上方；移除了原位置的重复定义。

## 验证结果
- `python -c "from main import app"` → FastAPI 应用加载成功，**41 条路由**，无任何错误
- `npx vite build` → 构建成功（index-BISBFk4J.js，333 KB）
- 部署包已同步，代码已推送至 GitHub master (`c1b6b7a`)

## 审计最终状态
**16/16 项已解决。** 项目现已具备完整闭环：数据模型 → DHCP 协议引擎 → Web 管理后台 → 部署脚本。
