# 2026-06-05 12:10 — #12 NTP/PXE + #13 Exclude Range 修复

## 完成内容

### #12：前端 NTP/PXE 配置入口
- **PoolManagement.tsx 新增 3 个 state**: `addNtp`, `addBootfile`, `addNextServer`
- **创建 Modal 新增字段**: NTP 服务器（逗号分隔）、PXE Next-Server（TFTP 地址）、Bootfile（引导文件名）
- **handleAdd payload**: 新增 `ntp_servers`, `bootfile`, `next_server` 字段
- **后端已有支持**: `PoolCreate` schema 已有 `ntp_servers`, `bootfile`, `next_server` 字段，无需修改

### #13：前端排除范围管理 UI
- **SubnetDraft 接口**: 新增 `excludes: Array<{start, end, reason}>` 
- **3 个 helper 函数**: `addExcludeRow`, `removeExcludeRow`, `updateExclude`
- **每子网底部显示排除范围区域**: 含 start/end/reason 输入 + 删除按钮，无范围时显示"无排除范围"
- **后端 SubnetCreate**: 新增 `excludes: Optional[List[ExcludeCreate]]`
- **create_pool / add_subnet**: 创建子网后遍历 excludes 创建 AddressExclude 行

### 国际化
- zh.json / en.json 新增 12 个键（ntp/bootfile/next_server/exclude ranges）

## 构建结果
- `npx vite build` 成功 → `index-BISBFk4J.js` (333.43 KB)

## Git
- commit `8426d2c`: 24 files, +1019/-102
- Push to GitHub master 成功 (`00c6cd9..8426d2c`)

## 审计维度状态速览

| # | 项 | 状态 |
|---|-----|------|
| 1 | DHCP 服务自启动 | ✅ lifespan start_dhcp_servers() |
| 2 | IP 分配游标算法 | ✅ next_ip_cursor + migration |
| 3 | DHCPv6 PD | ✅ 完整实现 |
| 4 | Schema 补全 reservation | ✅ |
| 5 | Redis 缓存 Dashboard | ✅ 30s TTL + invalidate |
| 6 | API 限流 | ✅ 60次/分钟/IP |
| 7 | CSV 流式导出 | ✅ async generator |
| 9 | DHCPv4 多 DNS | ✅ set_multi_ip_option |
| 10 | Server DUID-LLT | ✅ 自动生成+持久化 |
| 11 | CRUD 审计日志 | ✅ pools.py 全量 |
| 12 | 前端 NTP/PXE UI | ✅ 本次修复 |
| 13 | 前端排除范围 UI | ✅ 本次修复 |
| 14 | 连接池调优 | ✅ pool_size 80, overflow 40 |
| 16 | cleanup_expired v6 | ✅ v6 支持 + v4 回收 |
| 8 | 部署前 dist 存在 | ⚠️ 需验证 deploy-package |
| 15 | Dashboard 统计 API | ⚠️ 未审计，待确认 |

**全 16 项审计中仅剩 #8 和 #15 待验证，其余 14 项已修复。**
